# Reference only: this legacy application does not automatically load .env files.
# Set these variables in the PHP process environment before local use.
# Never reuse historical/production credentials or use real personal data.
DB_HOST=127.0.0.1
DB_USER=your_disposable_database_user
DB_PASSWORD=replace_with_local_database_password
DB_NAME=your_disposable_database_name
SECRET_KEY=replace_with_local_demo_key
DEFAULT_STAFF_PASSWORD=replace_with_local_staff_default
DEFAULT_STUDENT_PASSWORD=replace_with_local_student_default
